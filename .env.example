# ===== 环境变量模板（可以提交到 git）=====
#
# 这个文件里没有任何真实密钥，是给「克隆仓库的人」看的说明书：
# 告诉别人需要配哪些变量，但不会泄露值。
#
# 用法：
#   1. 复制成真正的配置文件：  cp .env.example .env
#   2. 把下面的空值填上真实内容
#   3. .env 已被 .gitignore 忽略，不会提交；本文件会被提交
#
# 生成一个随机密钥：
#   python -c "import secrets; print(secrets.token_hex(32))"

# --- JWT 签名密钥（必填）---
# 至少 32 字节。不填的话后端启动时会直接报错退出，不会用任何默认值兜底。
AUTH_SECRET_KEY=
